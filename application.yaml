eureka:
  client:
    service-url:
      defaultZone:

management:
  endpoints:
    web:
      exposure:
        include: "*"

#logging:
#  level:
#    lk.ijse.eca: DEBUG
#    org.springframework.web: INFO
#    org.hibernate.SQL: DEBUG